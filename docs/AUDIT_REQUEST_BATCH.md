# Заявка на аудит: пачка этапов P11, P12, P13, P16, P07 V2 и AI-часть P09

Аудитор — Codex. Автор реализации — Claude. Самопроверка автора **не** является независимым аудитом:
всё, что ниже, проверено только автором и тестами.

- Ветка: `dev/p07-watch-v2`, базовая точка — `main` = `7375c91` (v5.12.0, выпущено и работает на проде).
- Коммиты пачки: `91dd569` → `d37925c` (одна линия истории; последние четыре — отчёт аудита
  и исправления M01–M11).
- **Ничего не запушено.** На проде — v5.12.0; все выключатели новых возможностей стоят в положении «выкл».
- `schema_version` остаётся **5**: все таблицы добавочные, откат на v5.12.0 не требует восстановления базы.

## Как воспроизвести проверки

```bash
./venv/bin/python -m unittest discover -p "test_*.py"
```

Ожидаемо: **1151 тест OK** (после доработки остатков повторной приёмки). Браузерные наборы (нужны Deno и Chrome, см. `docs/AGENT_HANDOFF.md`):

```bash
for f in test_frontend*.cjs; do deno run -A --node-modules-dir=false "$f" || break; done
```

Ожидаемо: **14 наборов PASS**. Проверки шлюза выката запускаются отдельно:

```bash
./venv/bin/python release_gate.py secrets --root .
```

## Что проверять по этапам

### P11. Контролируемое включение адаптивного планировщика (`91dd569`)

Файлы: `scheduler_rollout.py`, `web/server.py` (`apply_adaptive_order`, `check_adaptive_rollback`,
`/api/admin/scheduler/rollout`), `web/templates/monitoring.html`, `test_scheduler_rollout.py`.

Заявленные гарантии:
1. Адаптивный порядок — всегда **подмножество** прежней волны: добавить магазин он не может.
2. Переход вперёд — только на один шаг; назад и в «выключено» — всегда.
3. Откат автоматический, расширение — только вручную владельцем.
4. Пороги согласованы владельцем **до** включения: рост доли ошибок или отказов по лимиту > ×1.33,
   падение полноты > 10 п.п., наблюдение 24 ч, минимум 50 запросов и 5 обходов.
5. Мало наблюдений — ни откат, ни «готов к шагу»; об этом говорится словами.

### P12. Помощник администратора (`5f50c44`)

Файлы: `admin_assistant.py`, `web/server.py` (`/api/admin/assistant`), `web/templates/monitoring.html`,
`test_admin_assistant.py`, `test_frontend_p12.cjs`.

Заявленные гарантии:
1. Список инструментов закрытый и только на чтение — проверяется тестом по исходному коду инструментов.
2. Цифры собирает код; у каждого блока фактов есть источник и период.
3. Ответ модели с числом, которого нет в собранных фактах, не показывается.
4. Без AI сервис отвечает фактами; недоступный блок называется недоступным, а не исчезает.
5. Названия магазинов и категорий подаются как данные, а не как указания (правило P08).

### P13. Суточная сводка и её отправка (`fe7169e`, `17984ce`)

Файлы: `daily_digest.py`, `database.py` (`daily_reports`), `notifier.py` (`send_daily_digest`),
`config.py` (три настройки), `web/server.py`, `test_daily_digest.py`, `test_frontend_p13.cjs`.

Заявленные гарантии:
1. Цифры воспроизводятся повторным расчётом того же дня; один день — одна запись, пересказ не теряется.
2. События режутся по точным меткам времени; дневные агрегаты (поиск, расходы AI) хранятся по суткам UTC
   и при другом часовом поясе помечаются приблизительными.
3. Незакрытые сутки помечаются неполными и пересчитываются; тихие сутки называются отсутствием данных.
4. Пересказ модели отменяется целиком, если в нём есть число, которого нет в отчёте.
5. Отправка в Telegram выключена по умолчанию, уходит только о завершившихся сутках и **один раз за день**
   (своё пространство `alert_id` + `UNIQUE(alert_id, user_id)`); письмо бывшему администратору отменяется.

### P16. Шлюз выката (`2f3effa`)

Файлы: `release_gate.py`, `.github/workflows/docker-publish.yml`, `.github/workflows/ci.yml`,
`docs/dependency_allowlist.json`, `docs/RELEASE_AND_ROLLBACK.md`, `test_release_gate.py`.

Заявленные гарантии:
1. Публикация образа зависит от всех шести заданий; образ собирается и проверяется **до** публикации.
2. Провал любой проверки — ненулевой код возврата, а не предупреждение.
3. Пропуск уязвимости действует только с причиной и датой окончания; просроченный снова блокирует.
4. Репетиция обновления не трогает исходный файл базы и прямо называет, возможен ли возврат на прежний
   образ и какие данные теряются, если нет.
5. Секрет не попадает в отчёт проверки; файлы вне репозитория (git-ignored) не блокируют выкат.

Проверено автором на живых данных: репетиция обновления прогнана на базе, созданной кодом тега `v5.12.0`
(схема 5 → 5, запросы приложения работают, целостность ok); smoke прогнан против настоящего приложения.

### P07 V2. Наблюдения (`24dfc16`)

Файлы: `watches.py`, `database.py` (`watched_offers`, `evaluate_watches`, `claim_watch_digest`),
`notifier.py` (`send_watch_digest`, `_deliver_watch`), `web/templates/index.html`, `test_watches.py`,
`test_frontend_p07_v2.cjs`.

Заявленные гарантии:
1. Кнопка «следить» создаёт наблюдение за товаром в один клик; гостю она не предлагается.
2. «Сделка» и «разница цен» срабатывают на **записанный алерт ленты**, а не на собственную догадку;
   наблюдения за арбитражем проверяются после записи его алерта.
3. Условие проверяется на соответствие виду: невозможная комбинация отклоняется словами.
4. Режим «сводка» отправляет накопленное одним письмом; устаревшая цена в него не попадает, неудачная
   отправка возвращает сводку в очередь целиком.
5. Одно наблюдение создаёт не больше 25 заданий за один обход.

### P09, AI-часть (`3832dcc`)

Файлы: `catalog_ai.py`, `database.py` (`matching_pairs`, `_ai_allows_comparison`), `monitoring.py`,
`web/server.py`, `docs/golden_matching.json`, `test_catalog_ai.py`, `test_frontend_p09_ai.cjs`.

Заявленные гарантии:
1. Модель видит только спорные пары: явное противоречие фасовки или характеристик ей не показывается,
   уже признанный правилом товар не пересматривается.
2. Вердикт может **разрешить** сравнение, но не запретить.
3. Принимается только уверенное «да» (≥ 0.90); непонятный ответ считается отсутствием ответа.
4. Пороги согласованы владельцем **до** замеров (уверенность 0.90, точность 1.0, полнота 0.94) и записаны
   в `catalog_ai.py` и `docs/golden_matching.json`; тест падает, если они разойдутся.
5. На пути сравнения цен нет вызовов модели: пара запоминается, разбор идёт отдельной задачей порциями,
   очередь ограничена и чистится; сбой AI-части оставляет сравнение прежним.


## Исправления по аудиту Codex (M01–M11), 2026-09-23

Отчёт аудита — [BATCH_AUDIT_CODEX.md](BATCH_AUDIT_CODEX.md). Все одиннадцать замечаний исправлены с
регрессиями; ниже — что именно изменилось и каким тестом это закреплено.

| № | Что было | Что сделано | Регрессия |
|---|---|---|---|
| M01 | Состав пробного шага переизбирался при каждом вызове: ухудшившийся магазин молча заменялся другим | Состав выбирается один раз при переходе и хранится в `adaptive_scheduler_canary`; выбывший участник называется в состоянии, замена не подбирается; откат очищает состав | `AuditFixesTest.test_membership_is_fixed_at_start_and_does_not_follow_the_ranking`, `…test_worsened_member_stays_under_observation_and_is_rolled_back`, `…test_disappeared_member_is_named_not_replaced` |
| M02 | «После» считалось за скользящие сутки и включало успехи до включения | `metrics(shops, since, until)` с явными границами; HTTP — по часовым агрегатам (`http_metrics_by_shop_window`), начало округляется вверх до часа; порог достаточности считает только период шага | `…test_history_before_the_stage_does_not_mask_a_failing_canary`, `…test_old_observations_do_not_fill_the_sufficiency_threshold` |
| M03 | Общий `POST /api/admin/config` выставлял шаг включения мимо проверки перехода и снимка «до» | Настройки со своей процедурой (`adaptive_scheduler_stage`, `ai_matching_mode`) общим сохранением не принимаются — ответ 400 с указанием своего переключателя | `GuardedSettingsTest.test_general_config_endpoint_refuses_the_staged_settings` |
| M04 | Любое число до 12 считалось нумерацией и проходило проверку | Разрешение по синтаксису списка (маркер в начале строки), а не по величине; деление известного числа на сто больше не порождает ноль; в примечании прямо сказано, что проверка ловит выдуманные величины, а не перепутанные | `NumberVerificationTest.test_a_number_in_a_sentence_is_not_excused_as_numbering`, `…test_list_numbering_is_allowed_by_syntax`, `SummaryTest.test_small_invented_number_is_rejected_too` |
| M05 | Счётчики ухудшений и восстановлений равнялись длине списка примеров (максимум 5) | Счёт ведётся отдельно от примеров | `AuditFixesTest.test_event_counters_are_exact_for_any_number` |
| M06 | Неизвестная стоимость AI показывалась как `$0.0` | Четыре разных ответа: вызовов не было, цены не заданы, сумма по части вызовов, известный ноль — в карточке и в письме | `AuditFixesTest.test_calls_without_prices_mean_unknown_cost_not_zero`, `…test_partially_priced_calls_are_named_partial` |
| M07 | `shutil.copy2` терял подтверждённые транзакции из журнала WAL, проверка целостности этого не видела | Копия снимается через SQLite Backup API; сверх того конвейер запускает **сам прежний релиз** на обновлённой копии (`release_gate.py rollback`), потому что равенство версии схемы — условие необходимое, но не достаточное | `WalSnapshotTest.test_committed_wal_record_is_in_the_rehearsed_copy`, `…test_previous_code_is_checked_on_the_upgraded_copy` |
| M08 | Сканер секретов пропускал все каталоги с точки, включая `.github`; слово «example» в строке оправдывало настоящий ключ | Обходятся файлы, которые хранит git; служебные каталоги исключаются поимённо; заглушка распознаётся по самому значению | `SecretScanTest.test_workflow_files_are_scanned`, `…test_word_example_next_to_a_real_key_does_not_excuse_it` |
| M09 | Пакет со `skip_reason` и пустой отчёт считались успешной проверкой | Отчёт обязан быть полным и понятным: пустой, повреждённый, с пропущенным или без списка уязвимостей пакетом — блокирует публикацию | `DependencyAuditTest.test_skipped_package_is_not_a_clean_result`, `…test_empty_or_foreign_report_does_not_pass` |
| M10 | Пакет из 25 срабатываний уходил двумя письмами (21 и 4) | Сводка забирает все готовые срабатывания наблюдения (предел 500); список сокращается, счётчик — нет | `DigestDeliveryTest.test_a_full_run_batch_goes_out_as_one_message` |
| M11 | `confidence` округлялась до показа, и 0.8999 проходило порог 0.90 | Решение принимается по исходному значению, округление — только для показа (`show_confidence`); `True` вместо числа не считается уверенностью | `RoundingTest.test_just_below_the_threshold_is_not_accepted`, `…test_whole_path_keeps_the_value_below_the_threshold` |

Проверки после исправлений: **Python 1093 OK**, **frontend 14/14 PASS**, `release_gate.py secrets` — чисто.
Непроведённое из списка ниже не изменилось: прод, Docker, живой конвейер, реальные вызовы моделей и
Telegram, замер AI-части на живой модели.


## Доработка остатков после повторной приёмки (2026-09-24)

Повторная приёмка закрыла M05/M08/M09/M10/M11 и вернула шесть остатков. Они исправлены; ниже — что
изменилось и чем закреплено.

| № | Остаток по приёмке | Что сделано | Регрессия |
|---|---|---|---|
| M01 | Первый canary не ограничен friendly и включёнными магазинами; выключенный магазин оставался «участником» | `canary_shops` берёт только включённые источники (`allowed_shops`), для первого шага требует FRIENDLY и иначе отказывает словами (`StageRefused`); `missing_members` считает выключенного участника недоступным; отказ не ломает волну | `CanaryMembershipTest` (7 тестов): нет friendly, лучший кандидат выключен, участник выключен после старта, отказ виден в состоянии |
| M02 | Первый неполный час исключался из HTTP-метрик, и провал не приводил к откату | При включении снимаются счётчики текущего часа (`http_counters_for_hour`), и час старта считается дельтой; отчёт показывает реальное покрытие (`http_coverage`); провал обходов приводит к откату даже при малом числе запросов | `PartialHourTest` (5 тестов), в том числе воспроизведение аудита: старт 12:10, 100 провальных запросов в 12:20 → откат |
| M03 | Сбой подготовки оставлял этап включённым без baseline | Единственный путь `switch_stage`: проверка → подготовка (состав, baseline, счётчики, время — одной транзакцией) → активация настройки; при любой ошибке прежнее состояние восстанавливается | `AtomicSwitchTest` (6 тестов) и `SwitchApiTest`: сбой `scheduler_candidates` даёт 500, шаг остаётся `off` без baseline |
| M04 | Число из данных можно было приписать другому показателю | Модель не пишет чисел вовсе: она ставит ссылку `{блок.поле}`, значение подставляет сервис из тех же фактов. Своё число модели и ссылка на несуществующий показатель — отказ. Применено и к помощнику, и к суточной сводке | `test_a_real_number_cannot_be_attached_to_the_wrong_metric`, `test_number_written_by_the_model_itself_is_refused`, `test_reference_to_a_missing_metric_is_refused`, `test_a_known_number_attached_to_the_wrong_metric_is_refused` |
| M06 | Строка агрегата смешивала вызовы с ценой и без, и сумма выглядела полной | В `ai_usage` добавлен счётчик оценённых вызовов (`priced_requests`), сводка считает полноту по нему; строки, записанные до счётчика, называются неполными | `MixedCostTest` (6 тестов): оба порядка None → цена и цена → None для одного ключа, прежние строки, передача неполноты в письмо |
| M07 | Workflow проверял откат на исходной старой базе | `upgrade --keep` сохраняет обновлённую базу, и `rollback` запускается по ней; исходный файл не меняется | `UpgradeThenRollbackTest` (5 тестов), включая несовместимость, которая валит именно последовательность upgrade → rollback |

Проверки после доработки: **Python 1151 OK**, **frontend 14/14 PASS**, `release_gate.py secrets` — чисто.

### Что осталось ограничением, а не исправлением

- Ссылаться можно только на числовые поля фактов верхнего уровня блоков; числа внутри списков
  (примеры обходов, снижения цен) модель процитировать не может и описывает их словами.
- Без снимка счётчиков (состояние, созданное прежней версией) час начала шага по-прежнему не
  засчитывается; отчёт помечает это как `partial_hour: excluded`.
- Сводка наблюдения больше 500 накопленных срабатываний по-прежнему разбивается на письма.
- Живой прогон конвейера, реальный pip-audit, настоящие вызовы моделей и Telegram, замер AI-части
  на живой модели и production canary не выполнялись.

## Что НЕ проведено (и не выдаётся за проведённое)

- Независимый аудит — предмет этой заявки.
- Прод и Docker: образ не собирался и не запускался на этой машине (операции с NAS и контейнерами вне
  полномочий исполнителя), конвейер GitHub Actions ни разу не прогонялся с новыми заданиями.
- Настоящие вызовы моделей: во всех тестах провайдер подменён. Значит, **замер AI-части P09 на живой
  модели не делался** — до него режим обязан оставаться `off`/`shadow`.
- Настоящая отправка в Telegram: `telegram_api` подменён, формат письма живым ботом не проверялся.
- Поведение на реальном потоке: сколько срабатываний в жизни даст наблюдение за целым магазином и сколько
  спорных пар в сутки накопит AI-часть — неизвестно.
- Намеренная поломка проверки в реальном конвейере (процедура описана в `docs/RELEASE_AND_ROLLBACK.md`,
  но на `main` не выполнялась).

## Порядок после аудита

1. Замечания Codex исправляет автор реализации, каждое — с регрессией.
2. После приёмки — решение владельца о выпуске: версия, CHANGELOG, тег, push.
3. Первое включение любой новой возможности на проде — отдельное решение владельца; по умолчанию все
   выключатели стоят в «выкл».
