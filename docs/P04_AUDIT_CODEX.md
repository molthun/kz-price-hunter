# Независимый аудит P04 — Codex

## Итоговый точечный аудит E01 — 2026-09-20 12:56, Codex

**P04 READY по аудиту кода: E01/E02 закрыты. Пачка P03/P04/P05 принята по коду, не выпущена.**
Проверен HEAD `67b4f9dc3867aa83c1909848689f8c4822b4d718`, ветка dev/p05-settings, diff a467b18..67b4f9d. Рабочее дерево до аудита чистое.

- type_totals перенесён после фильтров shop/category/search и до выбора deal_type, сортировки и пагинации. Предикаты типов общие для разбивки и списка.
- Независимая матрица на временной БД StoreDealsDbTest: **81 комбинация** города/магазина/категории/поиска, включая пустой результат; для all/super/arbitrage счётчик равен total соответствующего запроса. limit=1, offset=1, price_asc не меняют разбивку.
- Для всех городов, Астаны и Алматы независимо сверены get_stats.total_store_deals/total_arbitrage с городской разбивкой витрины — совпадают.
- Полный Python suite с временным DATA_DIR: **432 теста, 33.247 с, OK**. Регрессии E02 и личных порогов также проходят.
- Frontend в этом точечном прогоне не повторялся: исправление затрагивает только вычисления БД и тесты; все 7 frontend-наборов независимо прошли на предыдущем a467b18, файлы UI с тех пор не менялись. Docker, прод и реальные адаптеры не проверялись.

Новых замечаний в проверенном исправлении нет. Изменены только отчёт и передача; код приложения, рабочая БД и прод не менялись, push не выполнялся. Следующий шаг — подготовка выпуска 5.10.0 и предусмотренные релизные проверки по отдельному поручению владельца. READY не означает DEPLOYED. Ниже история предыдущих аудитов.

## Повторный аудит E01/E02 — 2026-09-20 12:50, Codex

Проверен HEAD `a467b189adbd61adf0a93d5acfa2790939c04019`, ветка dev/p05-settings; исходное рабочее дерево чистое.
**E02 закрыто. E01 частично закрыто: осталась разбивка при дополнительных фильтрах. P04 IN_PROGRESS.**

- Исходное воспроизведение E01 (три исторических алерта одного предложения) теперь даёт stats.total_arbitrage=1 и deals(arbitrage).total=1. Личные пороги больше не меняют счётчик витрины.
- Исходное E02: после изменения текущей цены d2 на 250000 старые алерты на 100000 не попадают в витрину; арбитражные total и счётчик stats равны 0. Условие применено до дедупликации и подсчёта.

### Остаток E01 — P2: type_totals не учитывает фильтры списка

В database.py::get_store_deals блок type_totals стоит до фильтрации shop/category/search. Поэтому обещанная в передаче сверка при одинаковых фильтрах не выполняется.
Независимое воспроизведение на временной БД fixture CountersTest с _record_arbitrage_history():
`get_store_deals(city='Астана', search='no-such-product', deal_type='arbitrage')` возвращает
**total=0, deals=[], type_totals={'all': 6, 'super': 6, 'arbitrage': 1}**.
Аналогично не учитываются магазин и категория. В текущем UI поле type_totals напрямую не используется: это дефект нового API-контракта, а не подтверждённое визуальное расхождение текущего экрана. Глобальная плитка вправе оставаться общей по городу; её исправление подтверждено.

Рекомендация: считать type_totals после общих фильтров shop/category/search, но до выбора deal_type и пагинации. store_deals_counts(city) без дополнительных фильтров сохранит общие городские показатели. Если нужна отдельная глобальная разбивка, назвать и документировать её отдельно.
Приёмка: пустой поиск, отдельный магазин, категория и их комбинации; для каждого t из all/super/arbitrage type_totals[t] совпадает с total отдельного запроса того же t и тех же остальных фильтров. Пагинация и сортировка не меняют счётчики. Существующий тест проверяет только типы без этих фильтров.

Независимые проверки: **431 Python-тест, 33.501 с, OK; frontend 7/7 PASS** (Deno/Chrome, офлайн). Воспроизведения на временных данных, без реальных вызовов AI/Telegram. Docker, прод и ручная геометрия UI не проверялись. Код приложения не менялся, push не выполнялся.
Следующий шаг: Claude исправляет остаток E01 и добавляет регрессии фильтров, затем точечный аудит. P03/P05 остаются READY по коду.

## Первый аудит (история)

Дата: 2026-09-20 12:39, Asia/Almaty. Проверен diff `142515c..f1b02c2`, анализ docs/P04_UI_DATA_ANALYSIS.md и итоговая интеграция.
**Итог: IN_PROGRESS — E01/E02 требуют исправлений.**

## E01 — P2: арбитражный счётчик не совпадает с витриной

Место: database.py:1302 (get_stats), get_store_deals; web/templates/index.html::loadStats («из них … арбитраж»).

Общий счётчик предложений приведён к витрине, но total_arbitrage всё ещё считает строки отфильтрованных alerts, а витрина склеивает предложения по модели и магазину. Кроме того, первая величина зависит от личных порогов, вторая — нет. Поэтому новая подпись «из них» описывает другой набор данных.

Воспроизведено на временной БД fixture test_ui_data.CountersTest: для d2 созданы три MARKET_ARBITRAGE с одинаковой ценой 100000, прежней 300000, выгодой 200000; перед следующей записью предыдущие даты сдвигались на 25 ч (итоговые возраста 0/25/50 ч, допустимый повтор через сутки). **get_stats(city='Астана').total_arbitrage=3, get_store_deals(city='Астана', deal_type='arbitrage').total=1**. Все условия свежести соблюдены.

Рекомендация: считать разбивку «из них арбитраж» из того же набора после дедупликации, что и total_store_deals, с теми же городом/фильтрами. Личные счётчики алертов при необходимости оставить отдельно с точной подписью.
Приёмка: несколько исторических алертов одного предложения, два города, личное отключение арбитража/пороги — счётчик витрины совпадает с её арбитражным списком при одинаковых фильтрах. Не только общий total, но и разбивка.

## E02 — P1: витрина показывает цену отменённого алерта после изменения товара

Место: database.py:2046, запрос alerts в get_store_deals.

В запросе проверены возраст товара и основания, но отсутствует проверка `p.current_price = a.new_price`, уже используемая в _fetch_filtered_alerts. После изменения актуальной цены старый алерт продолжает попадать в витрину и может победить при выборе максимальной выгоды.

В том же временном сценарии после обновления products.current_price для d2 с 100000 до 250000 витрина арбитража всё ещё возвращает **d2, new_price=100000**. Предложение свежее; проблема именно в несоответствии цены алерта текущей цене товара. Это существовавший до P04 дефект, обнаруженный при предусмотренной P04 сверке БД → API → UI; новый общий счётчик также включает такую запись.

Рекомендация: исключать алерты, чья new_price не соответствует текущей цене, до дедупликации, подсчёта и сортировки. Согласовать условия актуальности с выдачей алертов. Не подменять цену без пересчёта основания, процента и выгоды.
Приёмка: арбитраж/скидка → цена товара изменилась → старый алерт отсутствует в deals и счётчиках; актуальная каталожная скидка остаётся со своей текущей ценой; свежий действующий алерт виден.

## Проверки

Проверенный итоговый HEAD `044602a2d2a48357907b7598b95dcf64065d4f3d`, ветка `dev/p05-settings`. Рабочее дерево до правок документов чистое. Во время подхвата передача обновилась с P03 на пачку P03/P04/P05; тесты и воспроизведения относятся к итоговому HEAD пачки.

Независимые проверки: Python discovery с отдельным временным DATA_DIR — **427 тестов, 33.397 с, OK**; все **7 test_frontend*.cjs PASS** через `/opt/homebrew/bin/deno run -A --node-modules-dir=manual` (Chrome headless, офлайн). Тесты не использовали рабочую БД. Frontend-наборы не подтверждают геометрию Tailwind; ручные проверки размеров экрана Claude независимо не повторялись. Docker, реальные AI/Telegram и прод не проверялись. Код приложения не менялся; push не выполнялся.

Подтверждены одним писателем бейджей, city-параметром, frontend-тестами ошибок API, вынесенным модулем форматирования и тестами его экранирования. Независимо воспроизведены E01/E02 через настоящие функции БД на временных данных, без сети.
Следующий шаг: Claude исправляет E01/E02, добавляет регрессии, затем повторный аудит P04. P03/P05 не переоткрывать без новых оснований.
