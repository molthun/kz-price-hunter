# Точечная приёмка H01/H03 — Codex, 2026-09-20

HEAD **8a77f5a**, исправление **5161233**, ветка `dev/p09-catalog-quality`. **H01/H03 закрыты; H02 закрыт ранее. P08 — READY по аудиту проверенной реализации.** Новых блокирующих замечаний к этому diff не найдено.

## Подтверждение

- **H01:** reserve_ai_call объединяет проверку и списание в транзакции BEGIN IMMEDIATE с условным UPDATE общей SQLite. Независимый тест: восемь потоков с отдельными соединениями одновременно резервируют последнюю единицу (барьер перед вызовом); результат — одно True и семь False, сохранённый счётчик 1. Регрессия реализации проверяет также двух исполнителей через настоящий ограничитель до транспорта. Проверки независимости user/internal проходят.
- **H03:** локальный отказ передаёт provider_called=False, роутер записывает provider=none и при budget_exhausted прекращает fallback. Независимо проверен настоящий роутер и ограничитель с подменой только низкоуровневых сетевых функций: лимит 1, Gemini возвращает None, запасной заблокирован → транспорт только Gemini, счётчик 1, provider_calls=1, errors=1, fallbacks=0. Ошибка относится только к реально вызванному транспорту.
- **Занятость:** при занятом ограничителе ни один транспорт не вызван, расход 0, provider_calls=0, errors=0. Внутренние записи запросов/пропусков допустимы и не выдаются за сетевые вызовы.
- **Разрешённый fallback:** лимит 2, Gemini возвращает None, OpenAI отвечает → два транспорта, расход 2, provider_calls=2, errors=1, fallbacks=1. Запасной провайдер резервирует квоту тем же способом.
- **Замер P09:** golden v2 содержит 53 пары; TP=16, FP=0, TN=35, FN=2, precision=1.0, recall=0.8889. Все сохранённые числовые результаты совпадают с независимым вычислением, оба пропуска описаны. Пороги 0.99/0.70 пройдены.

## Проверки и границы

Неизменяемый экспорт 8a77f5a: `/tmp/kz-h01h03.YtKh58`. Python из venv основного checkout, только временный DATA_DIR. Команда: `DATA_DIR=$(mktemp -d) /Users/molthun/Documents/kz-price-hunter/venv/bin/python -m unittest discover -s . -p 'test_*.py'` — **659 тестов, 42.907 с, OK**, лог `/tmp/kz-h01h03-tests.log`.

Frontend повторно не запускался: этот diff не меняет UI; предыдущая независимая проверка на e48ccb8 — 9/9 PASS (исторический результат). Реальные AI/Telegram, стоимость живых вызовов, Docker и прод не проверены. Конкурентная проверка использует отдельные соединения в потоках, не отдельные OS-процессы; межпроцессная сериализация следует из SQLite-транзакции, отдельный многопроцессный эксперимент не проводился.

Код приложения, рабочая БД и настройки не менялись. Изменены только этот отчёт и передача; без коммита/push/выпуска. READY означает приёмку кода в проверенном объёме, а не DEPLOYED. Следующий шаг для общей пачки — отдельное поручение владельца на подготовку выпуска либо следующую задачу; перед выпуском остаются релизные проверки целевого окружения. Автоматически выпуск не запускать.

---

## История предыдущих аудитов

# Повторный аудит H01/H02 — Codex, 2026-09-20

HEAD `e48ccb8`, исправления `aaa5fe6`. **P08 остаётся IN_PROGRESS. H02 закрыт в части маршрутизации; H01 закрыт частично, дополнительно найден H03.**

## H01 — P1: разделение расхода исправлено, атомарное ограничение квоты отсутствует

Раздельные ключи user/internal действительно независимы, публичные функции направлены в соответствующую аудиторию. Но `ai_service._limited_provider_call` делает `daily_budget_allows(scan)` и `_count_ai_call(...)` отдельными операциями/соединениями. Атомарен increment, а не проверка и резервирование квоты. В одном event loop между ними нет await, однако два потока/процесса с общей БД обходят предел.

Независимое воспроизведение на временной SQLite: лимит user=1, расход 0. Два потока запускают настоящий `_limited_provider_call` через отдельные asyncio.run. Обёртка `daily_budget_allows` вызывает настоящую проверку и синхронизирует оба результата барьером перед возвратом; транспорт — async mock. Оба результата разрешены, **транспорт вызван дважды, ai_calls_today('user')=2 при лимите 1**. Ни счётчик, ни операции БД не подменялись.

Рекомендация: объединить проверку и резервирование в одну транзакцию/условный atomic update общей БД, возвращать отказ без сетевого вызова. Приёмка: отдельные соединения/исполнители соревнуются за последнюю единицу — только один достигает транспорта; user/internal остаются независимыми, fallback получает квоту тем же способом. Ограничение гонки относится к нескольким исполнителям, а не к одной asyncio-задаче.

## H02 — закрыт: действующие функции используют роутер

Нормализация, консультант и классификация категорий вызывают `ai_router.run`. Регрессии публичных функций подтверждают запрет fallback для normalize, разрешённый fallback консультанта и учёт задач/токенов. Ранее воспроизведённый обход роутера устранён. Это не подтверждение живых провайдеров и не полная приёмка защиты всех промптов от injection.

## H03 — P2: заблокированный вызов считается обращением к провайдеру

Место: `ai_service._limited_provider_call` возвращает None до заполнения usage_sink при budget_exhausted/skipped_busy; `ai_router.run` трактует None как ошибку провайдера и пишет строку с его именем. `database.ai_usage` включает её в provider_calls.

Независимое воспроизведение с настоящим роутером и ограничителем, лимит user=1, auto с двумя фиктивными ключами: транспорт Gemini возвращает None, OpenAI запрещён исчерпанной квотой ещё до транспорта. **Фактические транспорты: ['gemini']; дневной счётчик=1; отчёт provider_calls=2, errors=2.** Низкоуровневые сетевые функции подменены; router, limiter и DB реальные. Значит отказ локального бюджета ошибочно выглядит как второе обращение и отказ провайдера.

Рекомендация: передавать структурированный результат admission (called/skipped, причина) до роутера. Отдельно считать запрос задачи и реальный сетевой вызов; не записывать budget/busy как отказ провайдера. Приёмка: последний разрешённый вызов+заблокированный fallback → provider_calls=1; busy без сети → 0; причины пропуска видны, не искажают здоровье провайдера.

Следующий точный шаг: Claude исправляет H01/H03, добавляет сквозные регрессии, затем повторный аудит P08. Выпуск общей пачки пока не рекомендован.

Проверки повторены независимо на экспорте `e48ccb8` в `/tmp/kz-reaudit.6zTfMv`, macOS, Python из venv основного checkout, отдельный временный DATA_DIR. Команда: `DATA_DIR=$(mktemp -d) /Users/molthun/Documents/kz-price-hunter/venv/bin/python -m unittest discover -s . -p 'test_*.py'`: **638 тестов, 41.996 с, OK**. Все 9 `test_frontend*.cjs` через Deno `run -A --node-modules-dir=manual` с `TEST_PYTHON` из venv — exit 0. Логи `/tmp/kz-reaudit-suite.log`, `/tmp/kz-reaudit-test_frontend*.cjs.log`. Тесты используют mocks сетевых границ, рабочая БД не затронута. Реальные AI/Telegram, Docker, нагрузка и прод не проверены. Код приложения не менялся; коммит/push/выпуск не выполнялись.

---

## История предыдущих аудитов

# Независимый аудит P08 — Codex

Объём: diff `ddd5617..af0c0c4`, проверен в `a6c68cc`.
**Итог: IN_PROGRESS — H01/H02 (P1) требуют исправления.**

## H01 — P1: бюджеты пользователей и фоновых задач используют общий расход

Место: `ai_router.py:budget_allows` (117), `ai_service.py:_count_ai_call` (222), `daily_budget_allows`.
Заявленная независимость бюджетов не реализована: оба класса читают один `ai_calls_today`; для внутренних задач лишь снижен порог до 70%. Пользовательские обращения способны заблокировать фоновые задачи при нулевом расходе последних, а фоновые уменьшают остаток пользователей.

Воспроизведение с временной БД: лимит 100, 70 вызовов `_count_ai_call()`; счётчик 70, `budget_allows('normalize') == False`, `budget_allows('query_parse') == True`. Функция учёта вообще не принимает аудиторию. Неважно, какая сторона сделала эти 70 обращений — результат одинаковый. Тест «раздельных бюджетов» проверяет разные пороги над общим счётчиком и потому проходит.

Рекомендация: отдельный учёт и лимиты user/internal, с атомарным резервированием реального вызова на границе провайдера, включая retries/fallback. Если нужен дополнительный общий потолок, обозначить его отдельно. Приёмка: исчерпание одной квоты не списывает вторую; конкурентные вызовы не превышают лимит; отказ и fallback учитываются по согласованной политике.

## H02 — P1: рабочие AI-пути обходят политику и учёт роутера

Место: `ai_service.py:ask_ai_consultant` (694), `normalize_product_titles_batch` (834–837), классификация категорий (около 950). Через `ai_router.run` переведён разбор поискового запроса, но названные действующие функции вызывают провайдеров напрямую. Реестр задач не управляет этими вызовами, а `usage_sink`/запись `ai_usage` устанавливаются роутером.

Воспроизведение: временная БД, `config.get_ai_config` подменён на включённый AI, `ai_provider='auto'`, два фиктивных ключа; эвристика `extract_canonical_key` возвращает None; оба транспорта — AsyncMock(None). Настоящий `normalize_product_titles_batch(['audit uncommon product'], for_scan=True)` вызвал **Gemini 1 раз, OpenAI 1 раз**, хотя для `normalize` fallback запрещён. `database.ai_usage()` после этого показывает **requests=0, provider_calls=0, fallbacks=0**. Это проверка маршрутизации с mocks, не замер реального расхода API. Код прямых путей подтверждает отсутствие установки usage_sink и записи учёта.

Явный выбор Gemini/OpenAI отдельно фильтруется в `_get_api_credentials`: обход явного выбора данным замечанием НЕ утверждается. Дефект fallback воспроизведён именно в auto.

Рекомендация: перевести действующие консультант/нормализацию/категории на единую политику и учёт, сохранив deterministic/cache и поведение без AI. Приёмка: проверять публичные функции сервиса, а не только `ai_router.run`; запрещённый fallback не вызывает второй транспорт, разрешённый виден в отчёте, реальные попытки/токены/неизвестная стоимость отражаются корректно. Проверки обработки недоверенного текста также должны проходить через эти действующие пути.

Следующий шаг: Claude исправляет H01/H02 и передаёт на повторный аудит. Реальная интеграция с платными провайдерами этими mock-проверками не подтверждается.

## Окружение и ограничения

Аудит Codex, 2026-09-20, снимок HEAD `a6c68cc` ветки `dev/p09-catalog-quality`, экспорт через `git archive` в `/tmp/kz-p09-audit.iozDg7`. При приёме рабочее дерево чистое. Python из `/Users/molthun/Documents/kz-price-hunter/venv/bin/python`; данные только временные. Полный прогон на этом снимке: **572 теста, 36.440 с, OK**. Команда: `DATA_DIR=$(mktemp -d) /Users/molthun/Documents/kz-price-hunter/venv/bin/python -m unittest discover -s . -p 'test_*.py'`. Все девять `test_frontend*.cjs` завершились с кодом 0 через Deno (`run -A --node-modules-dir=manual`, `TEST_PYTHON` указывает на venv); node_modules подключён из основного checkout. Полный Python-лог: `/tmp/kz-p09-suite.log`; frontend-логи: `/tmp/kz-p09-test_frontend*.cjs.log` (временные артефакты).

Сеть AI/Telegram в воспроизведениях подменена. Реальные платные вызовы и отправки не выполнялись. Docker, нагрузка, ручная визуальная приёмка и прод не проверены. Код приложения, рабочая БД, настройки и ключи не менялись; push/выпуск не выполнялись. Прохождение suites не закрывает приведённые ниже сквозные дефекты.
